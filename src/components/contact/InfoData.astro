---
const operations = [
	{
		icon: '/icons/operations.png',
		text: 'Operaciones',
		description: 'Seguimiento de envios y soporte logístico.',
		emailIcon: '/icons/email.png',
		email: 'operativo@jissenglobal.com',
	},
	{
		icon: '/icons/sells.png',
		text: 'Ventas',
		description: 'Cotizaciones y asesoria comercial.',
		emailIcon: '/icons/email.png',
		email: 'ventas@jissenglobal.com',
	},
	{
		icon: '/icons/bills.png',
		text: 'Facturación',
		description: 'Consultas sobre pagos y documentos.',
		emailIcon: '/icons/email.png',
		email: 'facturacion@jissenglobal.com',
	},
];
---

<section class="text-center mb-12">
	<h1 class="text-6xl font-thin text-blue-600 mt-16">
		¿Cómo podemos ayudarte?
	</h1>

	<div class="grid md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-3 gap-8 mb-12 w-full">
		{
			operations.map((item) => (
				<div class="bg-white p-6 rounded-lg shadow-md text-center mt-4 md:last:col-span-3 lg:last:col-span-1">
					<img src={item.icon} alt={item.text} class="w-[100px] md:w-[200px] mx-auto mb-4" />
					<h3 class="text-4xl sm:text-5xl md:text-5xl xl:text-6xl font-semibold mb-4">{item.text}</h3>
					<p class="text-xl lg:text-2xl xl:text-3xl w-8/12 mx-auto text-center text-gray-400">
						{item.description}
					</p>
					<img
						src={item.emailIcon}
						alt={item.text}
						class="w-20 mt-8 mx-auto mb-4"
					/>
					<p class="text-sm xl:text-2xl w-full mx-auto text-center text-gray-400">
						{item.email.toLowerCase()}
					</p>
				</div>
			))
		}
	</div>
</section>
